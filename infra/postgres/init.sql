-- Crea una base de datos por microservicio (los que persisten datos).
-- El microservicio de cotizaciones (market) no usa base de datos.
CREATE DATABASE tpi_users;
CREATE DATABASE tpi_orders;
CREATE DATABASE tpi_history;
